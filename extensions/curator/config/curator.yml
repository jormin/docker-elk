# Curator configuration
# https://www.elastic.co/guide/en/elasticsearch/client/curator/current/configfile.html

client:
  hosts:
    - elasticsearch
  port: 9200
  http_auth: elastic:9b7611fbf5b8ac2a

logging:
  loglevel: INFO
  logformat: default
